﻿@model ManageBloodTypes.Models.ThongTinCaNhanModels

@{
    ViewBag.Title = "Information";
    Layout = "~/Views/Shared/_Layout_Doctor.cshtml";
}

<main id="main" class="main">

    <div class="pagetitle">
        <h1>Thông Tin Cá Nhân @Html.Raw(Model.HoTen)</h1>
    </div>

    <section class="section profile">
        <div class="row">
            <div class="col-xl-4">
                <div class="card">
                    <div class="card-body profile-card pt-4 d-flex flex-column align-items-center">
                        <img src="@Model.HinhAnh" alt="Profile" class="rounded-circle">

                        @*<h3>
                                @if (Model.NgheNghiep.HasValue && Model.NgheNghiep.Value != 0)
                                {
                                    @Html.Action("GetNgheNghiepName", "Doctor", new { id = Model.NgheNghiep })
                                }
                                else
                                {
                                    <text>(Chưa có thông tin)</text>
                                }
                            </h3>*@
                    </div>
                </div>
            </div>

            <div class="col-xl-8">
                <div class="card">
                    <div class="card-body pt-3">
                        <!-- Bordered Tabs -->
                        <ul class="nav nav-tabs nav-tabs-bordered">
                            <li class="nav-item">

                                <a href="/Doctor/Search" class="btn btn-danger"> Quay Lại</a>
                            </li>

                        </ul>

                        <div class="tab-content pt-2">
                            <div class="tab-pane fade show active profile-overview" id="profile-overview">
                                <div class="row" style="padding-top: 20px;">
                                    <div class="col-lg-3 col-md-4 label">Giới tính:</div>
                                    <div class="col-lg-9 col-md-8">
                                        @if (Model.GioiTinh != true)
                                        {
                                            <span>Nữ</span>
                                        }
                                        else
                                        {
                                            <span>Nam</span>
                                        }

                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-lg-3 col-md-4 label">Ngày sinh:</div>
                                    <div class="col-lg-9 col-md-8">
                                        @if (Model.NgaySinh.HasValue)
                                        {
                                            <span>@Model.NgaySinh.Value.ToString("dd/MM/yyyy")</span>
                                        }
                                        else
                                        {
                                            <span>Không có thời gian</span>
                                        }
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-lg-3 col-md-4 label">Địa chỉ:</div>
                                    <div class="col-lg-9 col-md-8">
                                        @Html.Raw(Model.DiaChi),
                                        @if (Model.IDPhuong.HasValue && Model.IDPhuong.Value != 0)
                                        {
                                            @Html.Action("GetPhuongXaName", "Doctor", new { id = Model.IDPhuong })
                                        }
                                        else
                                        {
                                            <text>(Chưa có thông tin)</text>
                                        },
                                        @if (Model.IDQuan.HasValue && Model.IDQuan.Value != 0)
                                        {
                                            @Html.Action("GetQuanHuyenName", "Doctor", new { id = Model.IDQuan })
                                        }
                                        else
                                        {
                                            <text>(Chưa có thông tin)</text>
                                        },
                                        @if (Model.IDThanhPho.HasValue && Model.IDThanhPho.Value != 0)
                                        {
                                            @Html.Action("GetThanhPhoName", "Doctor", new { id = Model.IDThanhPho })
                                        }
                                        else
                                        {
                                            <text>(Chưa có thông tin)</text>
                                        }
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-lg-3 col-md-4 label">Tình trạng:</div>
                                    <div class="col-lg-9 col-md-8">
                                        @if (Model.TinhTrangHonNhan != true)
                                        {
                                            <span>Đã Kết Hôn</span>
                                        }
                                        else
                                        {
                                            <span>Độc Thân</span>
                                        }
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-lg-3 col-md-4 label">Số CCCD:</div>
                                    <div class="col-lg-9 col-md-8">@Html.Raw(Model.CCCD)</div>
                                </div>

                                <div class="row">
                                    <div class="col-lg-3 col-md-4 label">Ngày cấp:</div>
                                    <div class="col-lg-9 col-md-8">
                                        @if (Model.NgayCap.HasValue)
                                        {
                                            <span>@Model.NgayCap.Value.ToString("dd/MM/yyyy")</span>
                                        }
                                        else
                                        {
                                            <span>Không có thời gian</span>
                                        }
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-lg-3 col-md-4 label">Nơi cấp:</div>
                                    <div class="col-lg-9 col-md-8">
                                        @if (Model.NoiCap_IDTP.HasValue && Model.NoiCap_IDTP.Value != 0)
                                        {
                                            @Html.Action("GetThanhPhoName", "Doctor", new { id = Model.NoiCap_IDTP })
                                        }
                                        else
                                        {
                                            <text>(Chưa có thông tin)</text>
                                        }
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-lg-3 col-md-4 label">Nhóm máu:</div>
                                    <div class="col-lg-9 col-md-8">
                                        @if (Model.IDNhomMau.HasValue && Model.IDNhomMau.Value != 0)
                                        {
                                            @Html.Action("GetNhomMauName", "Doctor", new { id = Model.IDNhomMau })
                                        }
                                        else
                                        {
                                            <text>(Chưa có thông tin)</text>
                                        }
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-lg-3 col-md-4 label">Số điện thoại:</div>
                                    <div class="col-lg-9 col-md-8"><strong>@Html.Raw(Model.SDT)</strong></div>
                                </div>
                            </div>
                        </div><!-- End Bordered Tabs -->
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>