@model IEnumerable<ManageBloodTypes.Models.BannerModel>

<section id="home-section" class="hero">
    <div class="home-slider owl-carousel">
        @foreach (var item in Model)
        {
            <div class="slider-item">
                <div class="overlay"></div>
                <div class="container-fluid px-md-0">
                    <div class="row d-md-flex no-gutters slider-text align-items-end justify-content-end" data-scrollax-parent="true">
                        <div class="one-third order-md-last img " style="background-image:url(@item.HinhAnh);">
                            @*<div class="overlay"></div>
                            <div class="overlay-1"></div>*@
                        </div>
                        @*<div class="one-forth d-flex  align-items-center ftco-animate" data-scrollax=" properties: { translateY: '70%' }">
                            <div class="text">
                                <span class="subheading">@item.TieuDe</span>
                                <h1 class="mb-4 mt-3">@Html.Raw(item.NoiDung)</h1>
                            </div>
                        </div>*@
                    </div>
                </div>
            </div>
        }
    </div>
</section>

